# 答辩准备 · 基于AI数字人和知识库的景区导游系统

> **答辩人：** 方家伟 · 计算机学院 软件工程专业 · 指导老师：王雪松

---

## 一、创新点

### 创新点① — GraphRAG 混合检索

**一句话：** 向量检索 + 图检索双路融合，召回率从 66.7% 提升到 93.3%。

用户问题进来后，系统同时走两条路：Milvus 做语义向量检索（L2 距离转相似度分 $s_{vec}$），Neo4j 做图关系检索（实体关系命中覆盖率 $s_{graph}$），再加一个实体一致性分 $s_{ent}$ 防止跨景区混淆。三分量线性加权得到综合得分 $S = 0.6\,s_{vec} + 0.25\,s_{graph} + 0.15\,s_{ent}$，权重经 15 道分层测试题离线调优得出。最终按 S 排序、在 2500 字符预算内取最优片段组装上下文送给 LLM。

纯向量只能做语义匹配，"景区有哪些景点"这类需要遍历 `HAS_SPOT` 图关系的问题它答不了；纯图检索又没有语义理解能力。两路融合互补，实验结果混合 93.3% vs 纯向量/纯图均约 66.7%。

**权重被问时：** α=0.6（语义最基础）、β=0.25（图结构补充关联）、γ=0.15（辅助过滤跨景区噪声），三权重在代码里会归一化，加和不为 1 也正常运行。

**代码入口：** [rag_service.py:1257](backend/app/services/rag_service.py#L1257) `hybrid_search` → 融合评分 [rag_service.py:1955](backend/app/services/rag_service.py#L1955) `_build_scored_context`

---

### 创新点② — 流式 TTS 文本切分

**一句话：** 遇到句末标点就切、异步并发合成，首帧延迟从约 7700ms 大幅降低。

原来的做法是等 LLM 把完整回答全部生成完，再整段提交讯飞 TTS，一篇 400 字的回答要等约 7700ms 才能开口说话。改进后，LLM 边流式输出边维护一个文本缓冲区，遇到句末标点（。！？. ! ?）且长度超过 12 字就立即切出一个子句，提交异步合成；合成完成后前端立刻播放，不等后续句子。多个子句最多 3 路并发合成（asyncio.Semaphore(3)），第一句合成完就开口，后续句子在播放期间已经在并行合成了。

合成失败时最多重试 3 次，指数退避（1s / 2s），鉴权失败直接抛出不重试。音频校验不通过（时长 < 0.15s 或文件 < 800 字节）则退回纯文本展示。

**代码入口：** [rag.py:436](backend/app/api/rag.py#L436) `_compute_cut`（切分逻辑）→ [rag.py:469](backend/app/api/rag.py#L469) `synthesize_and_store`（并发合成）→ [voice_service.py:166](backend/app/services/voice_service.py#L166) 讯飞 WebSocket TTS 实现

---

### 创新点③ — 多轮上下文维护

**一句话：** 主景点综合得分识别对话主体 + 1200 字符历史裁剪，多轮识别准确率 66.7%–83.3%。

每轮问答结束后，系统扫描用户问题 q 和 LLM 回答 r，对每个景点按 $Score = 1.0 \times cnt_q + 0.6 \times cnt_r + 0.8 \times \mathbb{I}_{景区归属}$ 打分，得分最高的景点作为当前主景点存入会话。用户下次说"这里""这附近"时，代词消解从最近 8 条历史提取实体名，拼到查询前再去检索，相当于把代词替换成具体景点名。

历史消息发给 LLM 前先裁剪：最多保留最近 20 条，再按 1200 字符预算两轮筛选——第一轮优先保留含景区名关键词的消息，第二轮把剩余预算补充最近的普通消息，保证 LLM 收到的上下文既有限又保留了最关键信息。

**代码入口：** [rag.py:190](backend/app/api/rag.py#L190) 主景点评分 → [rag.py:39](backend/app/api/rag.py#L39) `_trim_conversation_history` 历史裁剪

---

## 二、演示功能

### 功能① — 智能导览问答

**演示流程：** 打开游客端主界面（三栏布局：左导航 / 中 Live2D 数字人 / 右对话区）→ 文字提问"蜀南竹海有哪些主要景点" → 追问"其中哪个最值得游览"验证上下文保持 → 语音提问"开放时间"走完 ASR→GraphRAG→LLM→TTS 全链路。

**完整技术链路：** 前端录音上传 WAV → 后端 `POST /voice/transcribe` 用 Whisper base 模型识别为文字（[voice_service.py:63](backend/app/services/voice_service.py#L63)）→ `POST /rag/generate-stream` SSE 接口进入混合检索 → LLM 流式生成 → 边生成边切分提交讯飞 TTS → 前端收到音频 base64 立即播放，同时数字人口型同步。每次问答前根据 character_id 加载该角色的提示词和音色配置（[rag.py:118](backend/app/api/rag.py#L118)）。

**被问到 Whisper 时：** 模型加载在服务启动时完成（[voice_service.py:51](backend/app/services/voice_service.py#L51)），识别时用 `run_in_executor` 包装成异步不阻塞主线程，前端上传的音频落盘到系统临时目录，识别完立即删除（[voice.py:110](backend/app/api/voice.py#L110)）。

---

### 功能② — 景点知识库与检索

**演示流程：** 管理后台打开"检索调试"面板 → 输入"蜀南竹海有哪些景点" → 实时查看双路召回明细（Milvus 向量命中条目 + 相似度分值、Neo4j 图检索命中实体关系链）→ 观察混合评分排序 → 切换到知识条目管理页新增一条景点描述 → 验证后端自动向量化入库并同步更新 Neo4j 图谱。

**三库写入流程（新增一条知识时）：** 管理员提交文本 → `_upload_items_to_graphrag`（[admin.py:1029](backend/app/api/admin.py#L1029)）：① 调用 Embedding 模型生成 384 维向量 → `collection.insert` 写入 Milvus（[admin.py:1053](backend/app/api/admin.py#L1053)）；② LLM 结构化解析文本为 JSON（[rag_service.py:226](backend/app/services/rag_service.py#L226)）→ `build_scenic_cluster` 在 Neo4j 建景区簇节点和关系（[graph_builder.py:45](backend/app/services/graph_builder.py#L45)）；③ PostgreSQL 由 Prisma ORM 在各业务接口独立维护，是数据源。三步顺序执行，任一失败返回错误，管理员重试可重新同步。

**被问到一致性时：** 顺序写入 + 失败返回错误，最终一致性而非强一致性，原型系统的工程权衡，知识库人工维护频率低，偶发失败手动重试即可。

---

### 功能③ — 系统管理与权限

由于时间关系此功能不作现场演示，核心是两点：JWT 令牌认证（[auth.py:37](backend/app/api/auth.py#L37) `get_current_user` 作为 FastAPI 依赖注入到所有管理端接口），以及游客端和管理端权限硬隔离（游客只能访问问答与语音接口，管理端接口额外校验 `is_admin` 标志）。数字人角色支持完整增删改查，每个角色可独立配置讲解风格、TTS 音色、系统提示词和 Live2D 形象，启用开关即时生效。

---

## 三、答辩 Q&A（5 分钟问答）

**Q1：15 道测试题，样本太少，结论可靠吗？**

15 道题按三种典型问法分层构造：直接查询（5 道）、关联推理（5 道）、多跳问答（5 道），覆盖了实际使用中 90% 以上的问法。三种方法在同一测试集对比，相对结论可信。样本规模受限于毕设资源，扩展到更大规模是展望方向之一。

**Q2：三库写入失败怎么办，一致性怎么保证？**

顺序写入：Milvus → Neo4j，PostgreSQL 是数据源本身已持久化。任一步失败接口返回错误，管理员重试重新同步。三库不支持跨库分布式事务，当前是最终一致性，知识库由管理员人工维护、写入频率低，这是合理的工程权衡。

**Q3：TTS 首帧延迟从 7700ms 降到了多少？**

答辩前实测一次填入：**实测约 _____ ms**。优化原因：原来等完整回答全部生成才合成，改为遇到第一个句末标点就切出去异步合成，第一句合成完立刻播放，后续句子在播放期间并行处理。
