# 基于 GraphRAG 的 AI 智能导游系统 — 答辩准备文档

---

## 一、系统整体介绍（1 分钟口头稿）

本毕设实现了一个面向景区的 AI 智能导游系统。游客可以通过语音或文字提问，系统自动检索景区知识库并由大语言模型生成自然语言回答，同时支持语音播报。

系统的核心创新点是采用了 **GraphRAG（图增强检索生成）** 技术路线——将传统向量检索与知识图谱检索结合，通过线性融合评分选出最相关的上下文片段，再交给 LLM 生成回答，解决了纯向量检索语义单一、无法利用景点关联结构的问题。

---

## 二、技术架构

### 2.1 整体技术栈

| 层次 | 技术 |
|------|------|
| 后端框架 | FastAPI（异步，Python） |
| 关系数据库 | PostgreSQL（ORM: SQLAlchemy + Prisma） |
| 向量数据库 | Milvus（ANN 向量检索） |
| 图数据库 | Neo4j（知识图谱） |
| Embedding 模型 | `paraphrase-multilingual-MiniLM-L12-v2`（384 维） |
| 大语言模型 | OpenAI 兼容 API（可对接国内模型） |
| 语音识别（ASR） | OpenAI Whisper（本地，base 模型） |
| 语音合成（TTS） | 科大讯飞 TTS 云 API |
| 会话存储 | 内存（进程内，2小时超时自动清理） |
| 前端 | 游客端 + 管理端（两个独立 SPA） |
| 部署 | Docker Compose |

### 2.2 数据流（一次问答的完整路径）

```
用户问题（文字/语音）
    ↓
[语音识别] Whisper ASR（如为语音输入）
    ↓
[意图分类] 关键词快速分类 → 未命中则 LLM 分类
    ↓
[混合检索] Milvus 向量检索 + Neo4j 图检索（并行）
    ↓
[融合评分] S(c) = α·svec + β·sgraph + γ·sent
    ↓
[上下文裁剪] 字符预算 2500 字，取评分最高片段
    ↓
[会话历史拼接] 最近对话（1200 字符预算裁剪）
    ↓
[LLM 生成] 流式输出（SSE）+ System Prompt 约束
    ↓
[TTS 合成] 按句切分，并发发送讯飞 TTS，流式播报
    ↓
用户收到回答（文字 + 语音）
```

---

## 三、核心功能详解

### 3.1 知识图谱构建

**文件：** `backend/app/services/graph_builder.py`、`backend/app/services/rag_service.py`（`parse_scenic_text` / `parse_attraction_text`）

#### 图谱结构

知识图谱以**景区（ScenicSpot）**和**景点（Attraction）**为核心节点，向外辐射。

**节点类型：**

| 节点标签 | 含义 | 关键属性 |
|----------|------|----------|
| `ScenicSpot` | 景区 | name, area, ticket_info, opening_hours, transport_info, service_facilities |
| `Attraction` | 景点 | id, name, description, location, category, latitude, longitude |
| `Feature` | 特色 | name |
| `Honor` | 荣誉 | name |
| `Category` | 类别 | name |
| `Province/City/County` | 行政区划 | name |
| `Text` | 原始文本片段 | id, content |
| `Image` / `Audio` | 媒体资源 | url |

**关系类型：**

| 关系 | 含义 |
|------|------|
| `ScenicSpot -[:HAS_SPOT]-> Attraction` | 景区包含景点 |
| `Attraction -[:属于]-> ScenicSpot` | 景点归属景区（双向冗余，加速查询） |
| `Attraction -[:HAS_FEATURE]-> Feature` | 景点特色 |
| `Attraction -[:HAS_HONOR]-> Honor` | 景点荣誉 |
| `Attraction -[:HAS_CATEGORY]-> Category` | 景点类别 |
| `Attraction -[:位于]-> County/City/Province` | 行政区划 |
| `County -[:隶属]-> City -[:隶属]-> Province` | 行政区划层级 |
| `Text -[:DESCRIBES]-> Attraction` | 文本描述景点 |

#### 知识入库流程

管理员在管理端上传景区/景点介绍文本后，系统执行两步操作：

1. **结构化提取（LLM）：** 调用 `parse_scenic_text` 或 `parse_attraction_text`，以 temperature=0.1 的低温度调用 LLM，将自然语言文本提取为结构化 JSON（包含名称、位置、特色、荣誉、票价等字段）。

2. **图谱写入（Neo4j）：** `GraphBuilder.build_attraction_cluster` 使用 Cypher 的 `MERGE` 语句幂等写入——节点已存在则更新属性，不重复创建。同时以 `MERGE` 建立关系边。

3. **向量入库（Milvus）：** 对同一文本生成 384 维 embedding，写入集合 `tour_knowledge`，`text_id` 字段记录对应 Neo4j 节点 ID，实现跨库关联。

**Milvus 索引配置（`milvus_client.py`）：**
```
index_type: IVF_FLAT
metric_type: L2
nlist: 1024（建索引分桶数）
nprobe: 10（检索时探测桶数）
```

---

### 3.2 GraphRAG 混合检索（核心亮点）

**文件：** `backend/app/services/rag_service.py`，入口方法 `hybrid_search`（约第 1200 行）

#### 3.2.1 意图分类

检索策略依赖于对用户问题意图的判断。系统实现了**两级意图分类**：

**第一级：关键词快速分类（`_fast_classify_intent`，0ms）**

优先用正则/关键词判断，避免不必要的 LLM 调用：

| 意图 | 触发关键词示例 | 检索策略 |
|------|--------------|----------|
| `route` | 路线、行程、一日游、怎么玩 | top_k=10，graph_depth=3，展开景区所有景点 |
| `listing` | 有哪些、有什么景点、景点有多少 | top_k=8，展开景区景点，最多30个 |
| `detail` | 介绍、门票、开放时间、历史 | top_k=3，graph_depth=1，只取1个景点簇 |
| `comparison` | 对比、区别、哪个更好 | top_k=8，不展开 |
| `location` | 在哪、位置、怎么走 | top_k=5，不展开 |
| `feature` | 特色、打卡、推荐、最佳时间 | top_k=6 |
| `general` | 寒暄、问候等 | 跳过 RAG 检索 |

**第二级：LLM 分类（`_classify_query_intent`）**

关键词未命中时，调用 LLM（temperature=0，max_tokens=20，timeout=3s），只让 LLM 输出一个英文单词，极低延迟。

#### 3.2.2 向量检索（Milvus）

```python
# rag_service.py: vector_search()
query_vector = [self.generate_embedding(query)]  # 384 维
search_params = {
    "metric_type": "L2",
    "params": {"nprobe": 10},
}
results = collection.search(
    data=query_vector,
    anns_field="embedding",
    param=search_params,
    limit=top_k,
    output_fields=["text_id"],
)
# L2 距离转相似度：score = 1 / (1 + distance)
```

向量检索和意图分类通过 `asyncio.gather` **并行执行**，节省时间。

#### 3.2.3 图检索（Neo4j）

根据从查询文本中提取的实体（jieba 分词，词性标注），批量查询 Neo4j：

```python
# _graph_search_many：单次 Cypher 替代 N 次串行查询
UNWIND $names AS name
CALL {
  WITH name
  MATCH (a)-[r]->(b)
  WHERE a.name CONTAINS name OR b.name CONTAINS name
  ...
  LIMIT $per_limit
}
```

对多实体（listing/route 意图）还会执行 **子图查询**（`graph_subgraph_search`），以 1~3 跳路径拉取实体间关联结构（最多 50 条记录，深度上限 3）。

#### 3.2.4 融合评分（核心公式）

**文件：** `rag_service.py`，方法 `_build_scored_context`（约第 1800 行）

$$S(c) = \alpha \cdot s_{vec}(c) + \beta \cdot s_{graph}(c) + \gamma \cdot s_{ent}(c)$$

**默认权重（`rag_service.py` 顶部常量）：**
```python
DEFAULT_FUSION_ALPHA = 0.60   # 向量相似度权重
DEFAULT_FUSION_BETA  = 0.25   # 图结构支持权重
DEFAULT_FUSION_GAMMA = 0.15   # 实体一致性权重
```

**三个子分量的计算方式：**

| 分量 | 计算方式 |
|------|----------|
| $s_{vec}$ | Milvus 返回的 L2 距离转换：`1 / (1 + distance)`，范围 (0,1] |
| $s_{graph}$ | 图检索命中关系的端点名称在候选片段中出现次数的归一化值：`min(1.0, hits / max(3, len(rel_pairs)×2))` |
| $s_{ent}$ | 候选片段是否命中当前景区名称（1.0）或解析实体（1.0）或查询实体（0.5） |

**上下文字符预算：** 评分排序后，按 2500 字符预算贪心选取片段，超出则截止。

#### 3.2.5 指代消解

当用户问题含有"这个景区"、"这里"等指代词时（`_has_pronoun_reference` 检测），系统从近 8 条对话历史中提取景区/景点名（jieba 提取，过滤停用词），将其注入 `effective_query` 再检索：

```python
effective_query = f"{' '.join(resolved_entities[:2])} {query}"
```

---

### 3.3 多轮对话会话管理

**文件：** `backend/app/services/session_service.py`、`session_store.py`

#### 会话生命周期

```
创建 Session（UUID）→ 存储到内存
    ↓
每次对话：add_message(role, content)
    ↓
超出 max_history×2（20条）时滚动截断，保留最近 20 条
    ↓
2 小时无操作自动超时删除
```

**存储方式：** 内存存储，进程内字典维护，2 小时无操作自动清理过期会话。

#### 历史裁剪（`_trim_conversation_history`，`rag.py`）

发送给 LLM 前对历史做**字符预算裁剪（1200 字符）**，避免 context 过长：

1. 从最近 20 条消息中，**优先保留**包含当前景区名等关键实体的消息（`prefer_terms` 参数）
2. 剩余预算填充最近消息（从后向前）
3. 保持原有时间顺序输出

---

### 3.4 语音交互

**文件：** `backend/app/services/voice_service.py`、`backend/app/api/voice.py`

#### ASR（语音识别）

- 主选方案：**OpenAI Whisper**（本地运行，base 模型）
- 备选方案：**Vosk**（离线）
- 依赖 ffmpeg 进行音频格式转换
- 在 `__init__` 中检测 ffmpeg 可用性，不可用时 Whisper 降级禁用

#### TTS（语音合成）

- 使用**科大讯飞 TTS 云 API**
- 并发控制：`asyncio.Semaphore`，最大并发数由 `XFYUN_TTS_MAX_CONCURRENCY` 配置（默认 4）
- 输出前过滤 emoji 和 Markdown 特殊符号（`_strip_emoji`、`_clean_special_symbols`），防止 TTS 朗读异常

#### 流式 TTS 切分（`rag.py`，`generate_answer_stream`）

流式生成时，LLM token 逐步输出，系统按**句末标点**（。！？.!?）切分文本块，满足最小长度 `Lmin=12` 后提交 TTS，并发度 `TTS_MAX_CONCURRENCY=3`。前端收到音频 base64 后立即播放，实现"边说边播"效果。

---

### 3.5 流式响应（SSE）

**文件：** `backend/app/api/rag.py`，接口 `/generate-stream`

使用 FastAPI 的 `StreamingResponse` + `AsyncGenerator` 实现 Server-Sent Events，每个事件是 JSON 格式：

```
data: {"type": "session_id", "content": "xxx"}   # 第一帧，返回 session_id
data: {"type": "text", "content": "片段文字"}     # LLM 流式 token
data: {"type": "audio", "content": "base64..."}   # TTS 音频块
data: {"type": "done"}                             # 结束标志
data: {"type": "error", "content": "..."}         # 错误
```

---

### 3.6 两级缓存

**文件：** `rag_service.py`，LRU + TTL 实现

| 缓存类型 | 容量上限 | TTL | 作用 |
|----------|----------|-----|------|
| Embedding 缓存 | 1024 条 | 1800 秒 | 相同文本不重复计算向量 |
| 向量检索缓存 | 256 条 | 300 秒 | 相同 (query, collection, top_k) 不重复查 Milvus |

实现为 `OrderedDict` LRU：超出容量时弹出最旧条目（`popitem(last=False)`）；命中时移到末尾（`move_to_end`）。

---

## 四、可能被追问的问题 & 标准答案

### Q1：为什么要用 GraphRAG？纯向量检索有什么问题？

**答：** 纯向量检索只能做语义相似度匹配，但景区问答场景有以下纯向量检索覆盖不到的需求：
1. **结构化关系查询：** "景区有哪些景点"需要图的 `HAS_SPOT` 关系，不是文本相似度能解决的
2. **路线推荐：** 需要遍历景点间的关联（同属一个景区），图结构天然支持
3. **多跳推理：** "这个景区的门票政策"需要 景点→景区→门票信息 的跨节点查询
4. **实体精度：** 向量检索可能返回语义相近但无关的内容，图检索通过精确 name 匹配补充精度

所以用线性融合 $S = 0.60 \cdot s_{vec} + 0.25 \cdot s_{graph} + 0.15 \cdot s_{ent}$ 结合两者优势。

---

### Q2：embedding 模型为什么选 `paraphrase-multilingual-MiniLM-L12-v2`？

**答：** 这个模型是专门针对**多语言语义相似度**优化的，支持中文，且输出 384 维向量，在精度和推理速度之间有较好平衡。相比 768 维的模型，Milvus 存储和检索开销减半。项目在本地运行（设置了 `HF_HUB_OFFLINE=1`），不依赖网络。

---

### Q3：意图分类为什么不直接用 LLM？

**答：** 主要原因是**延迟**。LLM 调用即使是最轻量的请求也有 200ms~1s 的网络往返延迟。关键词分类 0ms 命中，覆盖了大部分常见问题。只有关键词无法判断时（如问句结构复杂），才退化到 LLM 分类，且设置了 3 秒超时，失败则默认 `general`。

---

### Q4：融合评分的权重 α=0.6, β=0.25, γ=0.15 是怎么来的？

**答：** 这是根据景区问答场景的实际特点经验设定的：
- 向量相似度（0.6）权重最高，因为语义匹配是最基础的相关性保障
- 图结构支持（0.25）次之，补充结构关联信息
- 实体一致性（0.15）最低，作为辅助过滤（确保返回的片段和用户当前聊的景区相关）

三个权重会在 `_build_scored_context` 中归一化，加和为 1，改变任意一个不影响其他语义。

---

### Q5：会话历史怎么处理？不会 context 越来越长撑爆吗？

**答：** 有两层限制：
1. `SessionService` 内部维护 `max_history=10`，超出 20 条消息（10轮对话）时滚动截断，只保留最近 20 条
2. 发送给 LLM 前，`_trim_conversation_history` 再次按 1200 字符预算裁剪，优先保留含景区名的消息，剩余预算从最近消息往前填

所以实际发给 LLM 的 context 是有限且可控的。

---

### Q6：指代消解是怎么实现的？

**答：** 检测到"这个景区"、"这里"等代词后，从近 8 条对话历史合并文本，用 jieba 提取名词/地名（词性标注 ns/nr/nt/nz），过滤停用词和过短词，取最多 5 个实体。然后把这些实体拼在查询前面（`effective_query = "景区名 原始问题"`）再去检索，相当于把代词换成具体实体。

---

### Q7：Milvus 是什么？为什么不用普通数据库做向量搜索？

**答：** Milvus 是专门的向量数据库，支持**近似最近邻（ANN）搜索**。普通关系数据库存向量可以，但相似度搜索只能暴力遍历（O(n)），在数据量大时极慢。Milvus 使用 `IVF_FLAT` 索引，先把向量空间分成 1024 个桶（nlist=1024），检索时只探测最近的 10 个桶（nprobe=10），大幅减少计算量，支持毫秒级响应。

---

### Q8：Neo4j 图数据库中 Cypher 查询有没有注入风险？

**答：** 有防注入处理。对于动态拼接关系类型的场景（如 `graph_search` 中的 relation_type），代码用正则白名单校验：
```python
if re.match(r"^[A-Z_][A-Z0-9_]*$", rel_candidate):
    rel = rel_candidate
```
只允许大写字母和下划线，拒绝一切特殊字符。节点属性查询均使用参数化（`$name`），不拼接字符串。

---

### Q9：TTS 为什么要并发控制？

**答：** 讯飞 TTS 是云 API，有并发限制。如果不加限制，一个长回答可能切成十几个句子同时发请求，触发 API 限流。用 `asyncio.Semaphore(4)`（可配置）控制最多同时 4 个 TTS 请求，既利用并发提速，又不超限。

---

## 五、系统局限性（主动说更显自知之明）

1. **冷启动延迟：** Embedding 模型首次加载约 5~10 秒（后台线程异步加载，到来的请求用 `threading.Event` 等待）
2. **知识库依赖人工录入：** 知识准确性取决于管理员上传内容质量
3. **并发能力有限：** 定位为演示系统，低并发场景，未做水平扩展
4. **图谱覆盖度：** LLM 结构化提取在格式不规范的文本上可能漏字段

---

## 六、核心代码逐段讲解

### 6.1 融合评分公式（最核心，必须熟）

`app/services/rag_service.py` — `_build_scored_context`

```python
# 权重常量，定义在文件顶部
DEFAULT_FUSION_ALPHA = 0.60   # svec 权重：向量相似度
DEFAULT_FUSION_BETA  = 0.25   # sgraph 权重：图结构支持
DEFAULT_FUSION_GAMMA = 0.15   # sent 权重：实体一致性

def _build_scored_context(self, *, vector_results, graph_results,
                           entity_names, resolved_entities, scenic_name,
                           alpha=0.60, beta=0.25, gamma=0.15,
                           budget_chars=2500):
    # 权重归一化，三个数之和不为 1 也能正常运行
    ssum = a + b + g
    a, b, g = a / ssum, b / ssum, g / ssum

    for item in vector_results:
        svec = float(item.get("score") or 0.0)   # Milvus: 1/(1+L2距离)

        # sgraph：图检索的关系端点名称，有多少出现在当前候选片段里
        hits = sum(1 for an, bn in rel_pairs if an in content or bn in content)
        sgraph = min(1.0, hits / max(3, len(rel_pairs) * 2))

        # sent：候选片段是否和当前景区/对话实体相关
        if scenic_name and scenic_name in content:
            sent = 1.0
        elif any(e in content for e in resolved_entities):
            sent = 1.0
        elif any(e in content for e in entities[:5]):
            sent = 0.5
        else:
            sent = 0.0

        S = a * svec + b * sgraph + g * sent
        # 最终按 S 降序排列，按 2500 字符预算贪心选取片段
```

**讲解要点：** svec 来自 Milvus 距离转换，值越高越相似。sgraph 是图检索补充的"结构可信度"——如果图里查出了"蜀南竹海 HAS_SPOT 海中海"这条关系，而候选片段文本里也出现了"蜀南竹海"或"海中海"，则说明该片段与图结构吻合，得分上浮。sent 负责过滤"语义相似但景区不对"的情况，比如两个不同景区描述相似，但只有当前景区的片段 sent=1.0。

---

### 6.2 两级意图分类

`app/services/rag_service.py` — `_fast_classify_intent` + `_classify_query_intent`

**第一级：关键词匹配（0ms，覆盖大多数情况）**

```python
@staticmethod
def _fast_classify_intent(query: str) -> Optional[QueryIntent]:
    q = query.strip()
    route_kw = ("路线", "行程", "规划", "怎么玩", "一日游", "游览顺序", ...)
    if any(k in q for k in route_kw):
        return QueryIntent.ROUTE

    listing_kw = ("有哪些", "有什么景点", "有多少个", "景点列表", ...)
    if any(k in q for k in listing_kw):
        return QueryIntent.LISTING

    detail_kw = ("介绍", "详情", "门票", "开放时间", "历史", "故事", ...)
    if any(k in q for k in detail_kw):
        return QueryIntent.DETAIL

    # ... comparison / location / feature 同理 ...

    # 短句直接认为是 DETAIL（如"海中海"、"竹海博物馆"）
    if len(q) <= 12 and not any(c in q for c in ("？", "?", "吗", "呢")):
        return QueryIntent.DETAIL

    return None  # 无法判断，交给 LLM
```

**第二级：LLM 分类（仅关键词未命中时触发）**

```python
async def _classify_query_intent(self, query: str) -> QueryIntent:
    fast_result = self._fast_classify_intent(query)
    if fast_result is not None:
        return fast_result          # 直接返回，不调 LLM

    # LLM 只输出一个英文单词，temperature=0，max_tokens=20，timeout=3s
    response = await asyncio.to_thread(
        self.llm_client.chat.completions.create,
        model=settings.OPENAI_MODEL,
        messages=[
            {"role": "system", "content": INTENT_CLASSIFY_SYSTEM},
            {"role": "user",   "content": f"用户问题：{query}"},
        ],
        temperature=0,
        max_tokens=20,
        timeout=3,          # 超时直接 fallback，不阻塞主流程
    )
    # 从返回文本里解析出意图单词
    # 失败则 return QueryIntent.GENERAL
```

**讲解要点：** 两级设计的核心是性价比——90% 的常见问题被关键词匹配直接解决，延迟为 0；只有少数复杂问句才会多花一次 LLM 调用，且有 3 秒超时兜底。

---

### 6.3 意图驱动的检索策略

`app/services/rag_service.py` — `_get_search_strategy`

```python
def _get_search_strategy(self, intent: QueryIntent) -> Dict:
    strategies = {
        QueryIntent.ROUTE: {
            "top_k": 10,                      # 多取片段，路线需要多个景点信息
            "relevance_threshold": 0.1,        # 阈值放低，宁可多检也别漏
            "graph_depth": 3,                  # 三跳子图，找到景点间关联
            "expand_scenic_attractions": True, # 展开景区所有子景点
            "max_attractions": 15,
        },
        QueryIntent.LISTING: {
            "top_k": 8,
            "relevance_threshold": 0.15,
            "expand_scenic_attractions": True,
            "max_attractions": 30,             # 列举类，最多返回30个景点
        },
        QueryIntent.DETAIL: {
            "top_k": 3,                        # 只要最相关的3条
            "relevance_threshold": 0.1,
            "graph_depth": 1,                  # 只看直接关系
            "expand_scenic_attractions": False,
            "max_attractions": 1,              # 只取一个景点详情
        },
        # ... comparison/location/feature/general 类似 ...
    }
    return strategies.get(intent, strategies[QueryIntent.GENERAL])
```

**讲解要点：** 不同意图对检索的需求截然不同。问路线要尽可能多的景点信息（top_k=10，graph_depth=3）；问详情只需要精准匹配一个景点（top_k=3，depth=1）。意图分类的结果直接控制检索的"范围"和"深度"。

---

### 6.4 混合检索主流程

`app/services/rag_service.py` — `hybrid_search`

```python
async def hybrid_search(self, query, top_k=5,
                         conversation_history=None, scenic_name=None):

    # --- 指代消解 ---
    if self._has_pronoun_reference(query) and conversation_history:
        resolved_entities = self._extract_entities_from_history(conversation_history)
        effective_query = f"{' '.join(resolved_entities[:2])} {query}"
    else:
        effective_query = query

    # --- 意图分类 + 向量检索：并行执行，节省时间 ---
    intent, vector_results_raw = await asyncio.gather(
        self._classify_query_intent(query),
        self.vector_search(effective_query, top_k=10),
    )

    # --- 按意图取策略 ---
    strategy = self._get_search_strategy(intent)

    # --- 相关性过滤：低于阈值的片段丢弃，但至少保留 1 条 ---
    vector_results = [r for r in vector_results_raw
                      if r.get("score", 0) >= strategy["relevance_threshold"]]
    if not vector_results and vector_results_raw:
        vector_results = vector_results_raw[:1]   # force_at_least_one

    # --- 实体提取（jieba 分词） ---
    entities = self.extract_entities(query)
    entity_names = [e["text"] for e in entities]

    # --- 图检索：批量查询，单次 Cypher 替代 N 次串行 ---
    graph_results = await self._graph_search_many(
        entity_names[:5], per_entity_limit=per_entity_limit
    )
    # listing/route 意图额外做子图查询（1~3 跳）
    if len(entity_names) > 1 and graph_depth > 1:
        subgraph_data = await self.graph_subgraph_search(
            entity_names[:3], depth=graph_depth
        )

    # --- 从 Neo4j Text 节点拉取片段正文 ---
    text_contents = await loop.run_in_executor(
        None, self._get_text_contents_from_neo4j, text_ids
    )

    # --- 融合评分，构造上下文字符串 ---
    enhanced_results, scored_candidates = self._build_scored_context(
        vector_results=vector_results,
        graph_results=graph_results,
        entity_names=entity_names,
        resolved_entities=resolved_entities,
        scenic_name=scenic_name,
    )

    return {
        "enhanced_context": enhanced_results,   # 最终给 LLM 的上下文
        "intent": intent.value,
        "vector_results": vector_results,
        "graph_results": graph_results,
        "entities": list(unique_entities.values()),
        ...
    }
```

**讲解要点：** 整个检索流程最关键的设计是 `asyncio.gather` 并行——意图分类（可能要调 LLM）和向量检索（要走网络到 Milvus）同时发出，互不等待，节省约一半延迟。

---

### 6.5 Milvus 集合定义与索引

`app/core/milvus_client.py` — `create_collection_if_not_exists`

```python
fields = [
    FieldSchema(name="id",        dtype=INT64,        is_primary=True, auto_id=True),
    FieldSchema(name="text_id",   dtype=VARCHAR,      max_length=200),   # 关联 Neo4j 节点
    FieldSchema(name="embedding", dtype=FLOAT_VECTOR, dim=384),          # 384 维向量
]
schema = CollectionSchema(fields, "Tour guide knowledge base")
collection = Collection(collection_name, schema)

# IVF_FLAT 索引：先聚类（nlist=1024桶），检索时只探测 nprobe=10 个桶
index_params = {
    "metric_type": "L2",
    "index_type": "IVF_FLAT",
    "params": {"nlist": 1024},
}
collection.create_index("embedding", index_params)
```

向量检索时 L2 距离转相似度分：

```python
# rag_service.py: vector_search()
for hit in results[0]:
    score = 1 / (1 + hit.distance)   # distance=0 → score=1.0（完全相同）
                                      # distance↑  → score↓（越远越不相似）
```

**讲解要点：** `text_id` 字段是连接向量库和图数据库的"桥梁"——Milvus 只存向量和 text_id，通过 text_id 到 Neo4j 拉取对应的 `Text` 节点正文，再结合图检索结果一起送入融合评分。

---

### 6.6 图谱节点构建（知识入库）

`app/services/graph_builder.py` — `build_attraction_cluster`

```python
async def build_attraction_cluster(self, attraction_data, text_id, text, parsed):
    # 1. 用 MERGE 幂等写入景点节点（已存在则更新，不重复创建）
    await self._execute_query_async("""
        MERGE (a:Attraction {id: $id})
        SET a.name = $name, a.description = $description,
            a.location = $location, a.category = $category, ...
    """, {...})

    # 2. 关联到景区（双向关系保持一致）
    await self._execute_query_async("""
        MATCH (a:Attraction {id: $id})
        MATCH (s:ScenicSpot {scenic_spot_id: $scenic_spot_id})
        MERGE (a)-[:属于]->(s)   # 景点 → 景区
    """, {...})
    await self._execute_query_async("""
        MERGE (s)-[:HAS_SPOT]->(a)  # 景区 → 景点（双向冗余加速查询）
    """, {...})

    # 3. 行政区划三级结构：省 → 市 → 县
    await self._execute_query_async("""
        MERGE (prov:Province {name: $prov})
        MERGE (city:City     {name: $city})
        MERGE (county:County {name: $county})
        MERGE (city)-[:隶属]->(prov)
        MERGE (county)-[:隶属]->(city)
    """, {...})

    # 4. 特色、荣誉节点（批量 UNWIND 一次写入）
    await self._execute_query_async("""
        UNWIND $features AS fname
        MATCH (a:Attraction {id: $id})
        MERGE (f:Feature {name: fname, attraction_id: $id})
        MERGE (a)-[:HAS_FEATURE]->(f)
    """, {"features": ["天然竹林", "森林覆盖率93%", ...], "id": att_id})

    # 5. 绑定文本节点（向量库 ↔ 图库桥梁）
    await self._execute_query_async("""
        MERGE (t:Text {id: $text_id})
        SET t.content = $text
        MATCH (a:Attraction {id: $id})
        MERGE (t)-[:DESCRIBES]->(a)
    """, {"text_id": text_id, "text": text, "id": att_id})
```

**讲解要点：** 全程用 `MERGE` 而非 `CREATE`，保证幂等性——管理员重复上传同一景点不会产生重复节点。`Text -[:DESCRIBES]-> Attraction` 这条边是向量库和图库的连接点：Milvus 存 text_id，通过它找到 Neo4j 里的 Text 节点，再沿 DESCRIBES 边找到 Attraction，再往上找 ScenicSpot。

---

### 6.7 会话历史裁剪

`app/api/rag.py` — `_trim_conversation_history`

```python
def _trim_conversation_history(history, *, budget_chars=1200,
                                prefer_terms=None):
    # prefer_terms 通常是 [当前景区名]，优先保留含景区名的历史消息
    terms = [t.strip() for t in (prefer_terms or []) if t.strip()]

    def _hit(m):   # 消息是否命中关键实体
        return any(t in m.get("content", "") for t in terms)

    recent = history[-20:]   # 最多取近 20 条
    flags = [_hit(m) for m in recent]

    keep = set()
    used = 0

    # 第一轮：优先纳入命中景区名的消息
    for i in range(len(recent) - 1, -1, -1):
        if not flags[i]:
            continue
        ln = len(recent[i].get("role","")) + len(recent[i].get("content",""))
        if used + ln > budget_chars:
            continue
        keep.add(i)
        used += ln

    # 第二轮：剩余预算从最近往前填充普通消息
    for i in range(len(recent) - 1, -1, -1):
        if i in keep:
            continue
        ln = len(recent[i].get("role","")) + len(recent[i].get("content",""))
        if used + ln > budget_chars:
            continue
        keep.add(i)
        used += ln

    return [recent[i] for i in range(len(recent)) if i in keep]
    # 保持时间顺序输出
```

**讲解要点：** 两轮贪心——先把"有用的"（含景区名的消息）装进去，再用剩余空间填最近消息。这样既控制了 token 消耗，又不丢失当前对话上下文中最关键的实体信息。

---

### 6.8 流式 TTS 切分

`app/api/rag.py` — `generate_answer_stream` 内部

```python
Lmin = 12          # 片段最短长度，防止 TTS 产生大量极短请求
SENTENCE_PUNCT = ['。', '！', '？', '.', '!', '?']
TTS_MAX_CONCURRENCY = 3   # 并发发送 TTS 请求数上限
tts_sema = asyncio.Semaphore(TTS_MAX_CONCURRENCY)

def _compute_cut(buf: str) -> Optional[int]:
    """决定在哪里切断文本提交 TTS"""
    last = max(buf.rfind(p) for p in SENTENCE_PUNCT)   # 最后一个句末标点位置
    if last >= 0:
        return last + 1    # 在标点后切，保留完整句子
    if len(buf) >= Lmin:
        return len(buf)    # 没有句末标点但够长，强制切
    return None            # 继续等待积累

# LLM token 逐步流入，按句切分后异步发 TTS
async for chunk in llm_stream:
    accumulated_text += chunk
    cut = _compute_cut(accumulated_text)
    if cut:
        sentence = accumulated_text[:cut]
        accumulated_text = accumulated_text[cut:]
        # 异步发 TTS，返回 base64 音频，通过 SSE 推给前端
        async with tts_sema:
            audio_b64 = await voice_service.synthesize(sentence, voice=voice)
        yield f"data: {json.dumps({'type':'audio','content':audio_b64})}\n\n"
```

**讲解要点：** 实现了"边生成边播放"——LLM 还在生成后半段时，前端已经在播放前几句的语音。句末标点优先切分保证 TTS 输入是完整句子，读出来不会断句奇怪。Semaphore 防止并发爆炸触发讯飞 API 限流。

---

### 6.9 Embedding LRU 缓存

`app/services/rag_service.py` — `_cache_get/set_embedding`

```python
# 使用 OrderedDict 实现 LRU，容量 1024 条，TTL 1800 秒
self._embedding_cache: OrderedDict = OrderedDict()

def _cache_get_embedding(self, key: str):
    item = self._embedding_cache.get(key)
    if not item:
        return None
    payload, expires_at = item
    if expires_at > 0 and time.monotonic() >= expires_at:
        self._embedding_cache.pop(key, None)
        return None                          # 过期，视为不命中
    self._embedding_cache.move_to_end(key)  # 命中，移到末尾（LRU 更新）
    return payload

def _cache_set_embedding(self, key: str, payload):
    expires_at = time.monotonic() + 1800    # 30 分钟过期
    if len(self._embedding_cache) >= 1024:
        self._embedding_cache.popitem(last=False)  # 淘汰最旧的
    self._embedding_cache[key] = (payload, expires_at)
```

向量检索同样有缓存（TTL 300 秒，容量 256 条），缓存 key 为 `(query, collection_name, top_k, model_name)` 四元组。

**讲解要点：** 景区导览场景下，游客高频问同类问题（比如多人先后问"门票多少钱"），缓存直接命中，既省去 sentence-transformer 推理时间，又避免重复查 Milvus。`OrderedDict` 天然有序，头部是最旧的，尾部是最新的，LRU 实现只需两个操作：命中时 `move_to_end`，淘汰时 `popitem(last=False)`。
