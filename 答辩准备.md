# 答辩准备 · 基于AI数字人和知识库的景区导游系统

> **答辩人：** 方家伟 · 计算机学院 软件工程专业 · 指导老师：王雪松  
> **建议总时长：** 15–17 分钟（不含提问）

---

## 时间分配参考

| 章节 | Slide | 建议时长 |
|------|-------|---------|
| 开场 | 1–2 | 30s |
| 01 研究背景与意义 | 3–4 | 2 min |
| 02 系统设计与架构 | 5–6 | 3 min |
| 03 系统功能演示 | 7–11 | 6 min |
| 04 核心算法分析 | 12–13 | 3 min |
| 05 总结与展望 | 14–15 | 2 min |
| 致谢 | 16 | 20s |

---

## Slide 1–2 · 封面 & 目录（30 秒）

> 各位老师好，我是方家伟，来自计算机学院软件工程专业。我的毕业设计题目是《基于AI数字人和知识库的景区导游系统的设计与实现》，指导老师是王雪松老师。下面我将按照目录顺序，依次介绍研究背景、系统架构、功能演示、核心算法和总结展望，请老师们批评指正。

---

## Slide 3–4 · 01 研究背景与意义（2 分钟）

### 口头稿

**【行业背景】**  
随着旅游业的快速发展，景区数字化服务需求日益迫切。据中国旅游研究院《全国智慧旅游发展报告2024》显示，75% 的游客通过在线平台获取旅游信息，传统导游模式已无法满足需求。

**【政策驱动】**  
国家"十四五"文旅融合发展规划明确提出推进景区智慧化升级，鼓励 AI 语音导览、虚拟数字人等新型服务形态，为本系统的研发提供了政策支撑。

**【传统导览痛点】**  
目前景区导览存在三大痛点：第一，人工导游覆盖率低、成本高；第二，静态导览牌信息单一，无法交互；第三，游客体验差，语言障碍突出。因此，开发一个可随时访问、智能问答、体验沉浸的导游系统具有重要的实际价值。

---

## Slide 5–6 · 02 系统设计与架构（3 分钟）

### 口头稿

本系统采用**三层分层架构**。

**前端交互层**基于 Vue3 + Live2D，负责页面渲染、语音采集和数字人展示；**中间逻辑层**以 FastAPI 为核心，集成 GraphRAG 检索、ASR/TTS 语音处理和会话管理等模块；**后端数据层**由三类数据库协同构成。

三库的分工可以概括为一句话：**PostgreSQL 存结构，Milvus 查语义，Neo4j 查图谱**。

- **PostgreSQL**：存储景区/景点/用户等结构化业务数据
- **Milvus**：存储文本向量（384维），支持毫秒级语义检索
- **Neo4j**：构建知识图谱，存储景区、景点、特色、荣誉等实体及其关系

整个系统通过 **Docker Compose** 一键容器化部署，各服务解耦，便于迁移和运维。

### 重点数据
- Embedding 模型：`paraphrase-multilingual-MiniLM-L12-v2`，384 维，支持中文
- Milvus 索引：IVF_FLAT，nlist=1024，nprobe=10，毫秒级响应
- 三库通过 `text_id` 字段互相关联，实现数据一致性

---

## Slide 7–11 · 03 系统功能演示（6 分钟）

---

### Slide 8 · 功能① 智能导览问答（1.5 min）

#### 口头稿
游客端采用**三栏布局**：左侧为景区导航，中间为 Live2D 数字人，右侧为对话区。游客可以通过文字或语音与数字人实时交互。

系统的核心是基于 **SSE 流式接口**的问答链路：用户问题 → Whisper ASR（语音转文字）→ GraphRAG 混合检索 → LLM 流式生成 → 讯飞 TTS 语音合成 → 前端实时播放。全程支持多轮上下文维护，不会"忘记"前面聊过的内容。

#### 演示步骤
1. 浏览器打开游客端主界面
2. 文本提问：**"蜀南竹海有哪些主要景点？"** — 观察 Live2D 口型与语音同步
3. 追问：**"其中哪个最值得游览？"** — 验证多轮上下文保持能力
4. 语音提问：**"开放时间是什么？"** — 测试 ASR→TTS 全链路

---

### Slide 9 · 功能② 景点知识库与检索（1.5 min）

#### 口头稿
本页展示 **GraphRAG 双路检索**的透明化过程。管理后台实时显示每次检索的召回明细：Milvus 向量库命中哪些片段、得分是多少；Neo4j 图数据库命中了哪些实体关系链；最后混合评分后选出最优上下文传给 LLM。

知识库管理方面，管理员新增一条景点描述后，系统自动完成三件事：向量化并写入 Milvus、解析实体写入 Neo4j 图谱、结构化数据写入 PostgreSQL，**三库实时一致**。

#### 演示步骤
1. 管理后台打开**"检索调试"面板**
2. 输入 "蜀南竹海有哪些景点"，观察双路召回明细与评分排名
3. 切换至**知识条目管理页**，新增一条景点描述
4. 观察后台自动入库日志，验证三库同步更新

---

### Slide 10 · 功能③ 数字人展示与交互（1 min）

#### 口头稿
数字人模块使用 **Live2D Cubism Web SDK** 在浏览器端实时渲染，配合 Vue3 响应式状态管理角色选择，支持热切换不刷新页面。

系统内置三种导游角色，实现**文本内容、语音音色、形象动作三者同步联动**：
- 甜美小妹 → TTS 音色"小露"，轻松活泼风格
- 专业学者 → TTS 音色"许久"，严谨详细风格
- 亲切导游 → TTS 音色"小燕"，亲切温暖风格

#### 演示步骤
1. 游客端依次切换三个角色
2. 向同一问题提问，对比 TTS 音色与讲解风格差异
3. 展示 Live2D 口型与语音实时同步效果
4. 切换不同角色，展示 Live2D 形象与 TTS 音色同步联动效果

---

### Slide 11 · 功能④ 系统管理与权限（1 min）

#### 口头稿
管理端支持对数字人角色的**完整 CRUD 操作**：新增/编辑/删除角色，每个角色可配置讲解风格、TTS 音色（小露/许久/小燕）、System Prompt、Live2D 形象，启用开关即时生效。

权限方面，系统采用 **JWT 令牌认证**，管理端 API 全部受 Bearer Token 保护，游客端与管理端权限严格隔离，Token 过期自动跳转登录页，防止未授权访问。

#### 演示步骤
1. 展示管理端角色配置页，演示新增/编辑角色
2. 切换"专业学者"→"亲切导游"，向同一问题提问对比效果
3. 注销后尝试访问管理接口，展示 JWT 鉴权拦截

---

## Slide 12–13 · 04 核心算法分析（3 分钟）

### 口头稿

本系统研发了三项核心算法，这是整个系统技术含量最高的部分。

---

**① GraphRAG 混合检索（最核心）**

传统 RAG 只做向量检索，存在两个问题：一是结构化关系查不了（比如"景区有哪些景点"需要图的 `HAS_SPOT` 关系）；二是语义相似但景区不对的内容会被误召回。

本系统的融合评分公式：

$$S(c) = 0.60 \cdot s_{vec} + 0.25 \cdot s_{graph} + 0.15 \cdot s_{ent}$$

- $s_{vec}$：Milvus L2 距离转换，`1/(1+distance)`，衡量语义相关性
- $s_{graph}$：图检索命中关系端点在候选片段中的覆盖率，衡量结构关联
- $s_{ent}$：候选片段是否包含当前景区名或对话实体，防止跨景区混淆

**实验数据：** 15 道测试题，混合检索召回率 **93.3%**；纯向量约 66.7%，纯图约 66.7%，混合检索提升显著。

---

**② 多轮上下文维护**

当用户问"这里的门票多少钱"，"这里"指什么？系统通过两层机制解决：

- **指代消解**：检测代词，从近 8 条历史中用 jieba 提取景区/景点名，拼入查询
- **历史裁剪**：1200 字符预算，优先保留含当前景区名的消息，再从最近向前填充

多轮对话识别准确率 **66.7%–83.3%**。

---

**③ 流式 TTS 切分**

LLM 流式输出时，系统按**句末标点**（。！？）切分文本，满足最短 12 字符后立即发送讯飞 TTS，前端收到音频 base64 即刻播放，实现"边生成边播放"。首帧延迟从约 **7700ms 大幅降低**。

### 重点数字（被追问时用）
| 指标 | 数值 |
|------|------|
| GraphRAG 召回率 | 93.3% |
| 纯向量召回率 | 约 66.7% |
| 纯图召回率 | 约 66.7% |
| 回答准确率 | 88.7% |
| 答案相关性得分 | 0.912 |
| 忠实度得分 | 0.895 |
| 语音识别准确率 | 95.8% |
| 多轮识别准确率 | 66.7%–83.3% |
| 原始首帧延迟 | ~7700ms |
| 融合权重 α/β/γ | 0.60 / 0.25 / 0.15 |

---

## Slide 14–15 · 05 总结与展望（2 分钟）

### 口头稿

本次毕业设计完成了以下三项主要工作：

**第一**，构建了 PostgreSQL + Neo4j + Milvus **三库协同异构数据架构**，三库分别承担结构化存储、语义检索、图谱查询，通过 `text_id` 字段跨库关联，实现数据实时一致。

**第二**，研发了 GraphRAG 混合检索、多轮上下文维护、流式 TTS 切分三项核心算法，在召回率、多轮识别准确率、响应延迟等关键指标上均达到或超过设计目标。

**第三**，开发了覆盖游客端与管理端的完整可运行 Web 系统，支持文字与语音双模态交互，具备完善的 JWT 认证与权限控制机制。

**展望方面**，后续可从三个方向优化：一是引入用户画像实现个性化导览推荐；二是知识库支持多模态（图片、视频）；三是水平扩展以支持更高并发的景区实际部署场景。

---

## 答辩 Q&A · 标准答案

---

### Q1：为什么要用 GraphRAG？纯向量检索有什么问题？

纯向量检索只能做语义相似度匹配，景区问答有它覆盖不到的场景：

1. "景区有哪些景点" 需要图的 `HAS_SPOT` 关系，语义检索解决不了
2. 路线推荐需要遍历景点间关联，图结构天然支持多跳查询
3. 两个不同景区描述相似时，向量检索可能返回错误景区的内容，而 $s_{ent}$ 分量能过滤掉

实验结果：纯向量约 66.7%，纯图约 66.7%，混合 **93.3%**，提升显著。

---

### Q2：融合权重 α=0.6, β=0.25, γ=0.15 怎么来的？

根据景区问答场景特点经验设定，并通过离线实验调优：
- 向量相似度（0.6）权重最高，语义匹配是最基础的相关性保障
- 图结构支持（0.25）次之，补充结构关联信息
- 实体一致性（0.15）作为辅助过滤

三个权重在 `_build_scored_context` 中会归一化，加和不为 1 也能正常运行。

---

### Q3：意图分类为什么不直接用 LLM？

**延迟**是核心原因。LLM 调用即使是轻量请求也有 200ms–1s 的网络往返。关键词分类 0ms 命中，覆盖大多数常见问题（路线/列举/详情/位置等）。只有关键词无法判断时才退化到 LLM 分类，且设置 3 秒超时，失败则默认 `general` 意图继续流程。

---

### Q4：会话历史不会越来越长撑爆 context 吗？

有两层限制：
1. `SessionService` 内部 `max_history=10`，超过 20 条消息时滚动截断
2. 发给 LLM 前，`_trim_conversation_history` 按 **1200 字符预算**再次裁剪，优先保留含景区名的消息

所以发给 LLM 的 context 始终有限且可控。

---

### Q5：指代消解怎么实现的？

检测到"这里""这个景区"等代词后，从近 8 条历史合并文本，用 jieba 提取名词/地名（词性 ns/nr/nt/nz），过滤停用词和过短词，取最多 5 个实体，拼接到查询前：

```python
effective_query = f"{' '.join(resolved_entities[:2])} {query}"
```

相当于把代词替换成具体实体再检索。

---

### Q6：TTS 为什么要并发控制？

讯飞 TTS 是云 API，有并发限制。不加控制时，一个长回答可能切成十几句同时发出，触发 API 限流。用 `asyncio.Semaphore(4)` 控制最多 4 个并发请求，既利用并发提速，又不超限。

---

### Q7：Milvus 和普通数据库做向量搜索有什么区别？

普通关系数据库存向量可以，但相似度搜索只能暴力遍历（O(n)），数据量大时极慢。Milvus 使用 IVF_FLAT 索引，先把向量空间分成 1024 个桶（nlist=1024），检索时只探测最近的 10 个桶（nprobe=10），大幅减少计算量，支持毫秒级响应。

---

### Q8：Neo4j 查询有没有注入风险？

有防注入处理。对于动态拼接关系类型的场景，用正则白名单校验：

```python
if re.match(r"^[A-Z_][A-Z0-9_]*$", rel_candidate):
    rel = rel_candidate
```

节点属性查询均使用参数化（`$name`），不拼接字符串。

---

### Q9：系统有哪些局限性？（主动说，展示自知之明）

1. **冷启动延迟**：Embedding 模型首次加载约 5–10 秒（后台线程异步加载，请求用 Event 等待）
2. **知识库依赖人工录入**：准确性取决于管理员上传内容的质量
3. **并发能力有限**：定位为演示系统，未做水平扩展，低并发场景适用
4. **图谱覆盖度**：LLM 结构化提取在格式不规范文本上可能漏字段

---

## 关键代码速查（被追问时翻）

---

### 1. GraphRAG 融合评分公式
**文件：** `backend/app/services/rag_service.py` · 第 33–36 行（权重定义）+ 第 1955–2014 行（评分实现）

**口头稿：**
> 这里是 GraphRAG 混合检索的核心评分代码。三个权重 alpha、beta、gamma 以常量形式定义在文件顶部，分别对应向量分、图结构分和实体一致性分。评分函数 `_build_scored_context` 先对三个权重做归一化，再对每个候选片段分别计算三个分量，最后加权求和得到综合得分 S，取分数最高的片段组装成上下文送给 LLM。

```python
# rag_service.py · 第 33–36 行：权重定义
# 线性融合评分默认权重 S(c)=αsvec+βsgraph+γsent 与上下文字符预算
DEFAULT_FUSION_ALPHA = 0.60
DEFAULT_FUSION_BETA  = 0.25
DEFAULT_FUSION_GAMMA = 0.15
DEFAULT_CONTEXT_BUDGET_CHARS = 2500
```

```python
# rag_service.py · 第 1955–2014 行：评分实现（_build_scored_context 内）
# ① 归一化三权重，防止权重之和不为 1 时结果失真
a, b, g = alpha, beta, gamma
ssum = a + b + g
if ssum <= 0:
    a, b, g = 1.0, 0.0, 0.0
else:
    a, b, g = a / ssum, b / ssum, g / ssum

# ② 对每个向量检索候选片段计算三个分量
for item in vector_results:
    svec  = float(item.get("score") or 0.0)          # 向量相似度（由L2距离换算）

    hits  = sum(1 for an, bn in rel_pairs             # 图检索命中实体在片段中出现次数
                if (an and an in content) or (bn and bn in content))
    sgraph = min(1.0, hits / max(3, len(rel_pairs)*2))# 归一化为命中覆盖率

    sent  = 0.0                                       # 实体一致性分
    if scenic_name and scenic_name in content:
        sent = 1.0                                    # 景区名命中 → 满分
    elif any(e in content for e in resolved):
        sent = 1.0                                    # 消解实体命中 → 满分
    elif any(e in content for e in entities[:5]):
        sent = 0.5                                    # 备用实体命中 → 半分

    S = a * svec + b * sgraph + g * sent              # 线性加权综合得分
```

---

### 2. 历史窗口裁剪
**文件：** `backend/app/api/rag.py` · 第 28–85 行

**口头稿：**
> 为了防止多轮对话历史无限膨胀撑爆 LLM 的上下文窗口，我在发送给 LLM 之前做了两层裁剪：第一层是最多保留最近 20 条消息；第二层是按 1200 字符预算再次裁剪，且优先保留包含当前景区名等关键词的消息，这样既控制了长度，又不会丢失最关键的上下文信息。

```python
# rag.py · 第 28–29 行：预算参数
_HISTORY_BUDGET_CHARS = 1200   # 发送给LLM前的对话历史字符预算
MAX_RECENT_HISTORY    = 20

# rag.py · 第 39–85 行：裁剪函数
def _trim_conversation_history(
    history: List[Dict[str, str]],
    *,
    budget_chars: int = _HISTORY_BUDGET_CHARS,
    prefer_terms: Optional[List[str]] = None,   # 优先保留含这些词的消息（如景区名）
) -> List[Dict[str, str]]:
    recent = [m for m in history if m.get("role") in ("user", "assistant")]
    recent = recent[-MAX_RECENT_HISTORY:]       # 第一层：最多取最近 20 条
    flags  = [_hit(m) for m in recent]          # 标记含关键词的消息

    keep: set[int] = set()
    used = 0
    # 第二层 ①：优先放入含关键词的消息
    for i in range(len(recent) - 1, -1, -1):
        if not flags[i]: continue
        ln = _len_msg(recent[i])
        if used + ln > budget_chars: continue
        keep.add(i); used += ln
    # 第二层 ②：剩余预算从最近向前补充普通消息
    for i in range(len(recent) - 1, -1, -1):
        if i in keep: continue
        ln = _len_msg(recent[i])
        if used + ln > budget_chars: continue
        keep.add(i); used += ln

    return [recent[i] for i in range(len(recent)) if i in keep]
```

---

### 3. 主景点综合得分模型
**文件：** `backend/app/api/rag.py` · 第 190–237 行

**口头稿：**
> 多轮对话中"这里""这个景点"等代词指向的景点需要系统自动识别。这里的做法是对每个候选景点计算一个综合得分：用户问题中提到该景点名计 1.0 分，LLM 回复中提到计 0.6 分，该景点属于当前景区计 0.8 分，三项加权求和，得分最高的景点即为当前对话的主景点，后续追问时优先用它来消解代词。

```python
# rag.py · 第 190–231 行：主景点评分
wq, wr, ws = 1.0, 0.6, 0.8   # 问题提及权重 / 回复提及权重 / 景区归属权重

for rid, name in _get_attraction_id_name_list():
    cnt_q = q.count(name)     # 问题中景点名出现次数
    cnt_r = r.count(name)     # 回复中景点名出现次数
    if cnt_q == 0 and cnt_r == 0:
        continue

    # scenic_hit：该景点是否属于当前景区（通过 Neo4j 查询 HAS_SPOT 关系）
    scenic_hit = 1 if int(rid) in scenic_aid_set else 0

    score = wq * cnt_q + wr * cnt_r + ws * scenic_hit
    if score > best_score:
        best_score = score
        best_id    = int(rid)   # 综合得分最高的景点即为主景点
```

---

### 4. 流式 TTS 标点切分 + 异步并行合成
**文件：** `backend/app/api/rag.py` · 第 428–497 行

**口头稿：**
> 传统做法是等 LLM 输出完整句子再调用 TTS，导致首帧延迟高达约 7700ms。我的优化思路是：LLM 流式输出时，把文本按句末标点（句号、感叹号、问号）实时切分，每积累够 12 个字符且遇到句末标点就立即提交 TTS 合成，不等后面的文字。同时用 `asyncio.Semaphore` 控制最多 3 路并发 TTS 请求，前端收到 base64 音频就立刻播放，实现"边生成边说话"的效果，大幅降低了首帧延迟。

```python
# rag.py · 第 428–497 行：TTS 切分与异步并发

Lmin = 12                           # 触发切分的最短字符数
TTS_MAX_CONCURRENCY = 3             # 最多同时进行 3 路 TTS 合成
tts_sema = asyncio.Semaphore(TTS_MAX_CONCURRENCY)
SENTENCE_PUNCT = ['。', '！', '？', '.', '!', '?']

def _compute_cut(buf: str) -> Optional[int]:
    """有句末标点则在最后一个标点后切；否则满 Lmin 时强切。"""
    last = max(buf.rfind(p) for p in SENTENCE_PUNCT)   # 找最后一个句末标点
    if last >= 0:
        return last + 1            # 标点后切断
    if len(buf) >= Lmin:
        return len(buf)            # 无标点但够长时强切
    return None                    # 还不够长，继续等

async def synthesize_and_store(idx: int, original_txt: str) -> None:
    """异步 TTS 合成，通过 Semaphore 控制并发上限。"""
    async with tts_sema:           # 最多 3 路同时合成
        path = await asyncio.wait_for(
            voice_service.synthesize_xfyun(txt, voice=voice),
            timeout=TTS_SENTENCE_TIMEOUT,
        )
        if path and _validate_wav_file(path):
            with open(path, "rb") as f:
                b64 = base64.b64encode(f.read()).decode("utf-8")
            completed_audio[idx] = b64  # 存入完成队列，前端按序取走播放
```
