# 答辩准备 · 基于AI数字人和知识库的景区导游系统

> **答辩人：**方家伟 · 计算机学院 软件工程专业 · 指导老师：王雪松  
> **建议总时长：** 8分钟（含演示，不含提问）

---

## 时间分配参考

| Slide | 内容 | 建议时长 |
|-------|------|---------|
| 1 | 封面 | 20s |
| 2 | 目录 | — |
| 3 | 章节页：研究背景与意义 | 5s |
| 4 | 研究背景内容 | 55s |
| 5 | 章节页：主要工作与创新点 | 5s |
| 6 | 主要工作与创新点内容 | 30s |
| 7 | 章节页：系统设计与核心算法 | 5s |
| 8 | 系统设计 | 35s |
| 9 | 核心算法 | 45s |
| 10 | 章节页：系统功能演示 | 5s |
| 11 | 功能① 智能导览问答 | 80s |
| 12 | 功能② 景点知识库与检索 | 80s |
| 13 | 功能③ 数字人展示与交互 | 55s |
| 14 | 功能④ 系统管理与权限 | 10s |
| 15 | 章节页：总结与展望 | 5s |
| 16 | 总结与展望内容 | 35s |
| 17 | 致谢 | 10s |
| **合计** | | **≈ 8 min** |

---

## Slide 1 · 封面（20秒）

### 口头稿

各位老师好，我是方家伟，来自计算机学院软件工程专业，指导老师是王雪松老师。我的毕业设计题目是《基于AI数字人和知识库的景区导游系统的设计与实现》，下面请老师批评指正。

---

## Slide 2 · 目录

*（无需口头稿，切换到下一页即可）*

---

## Slide 3 · 章节页：研究背景与意义（5秒）

### 口头稿

首先介绍研究背景与意义。

---

## Slide 4 · 研究背景内容（55秒）

### 口头稿

据中国旅游研究院《全国智慧旅游发展报告2024》，75%的游客通过在线平台获取旅游信息，国家"十四五"政策也明确鼓励AI语音导览、虚拟数字人等新型服务形态。然而传统景区导览存在三大痛点：人工导游成本高、覆盖率低；静态导览牌信息单一、无法交互；语言障碍突出、游客体验差。因此，开发一个可随时访问、智能问答、体验沉浸的导游系统具有重要的实际价值，这就是本课题的出发点。

---

## Slide 5 · 章节页：主要工作与创新点（5秒）

### 口头稿

接下来介绍本系统的主要工作与创新点。

---

## Slide 6 · 主要工作与创新点内容（30秒）

### 口头稿

本系统完成了三项主要工作：构建三库协同异构数据架构、研发三项核心算法、开发完整可运行的Web系统。核心创新在于GraphRAG混合检索，通过融合向量检索与图检索，混合召回率达到93.3%，而纯向量或纯图均只有约66.7%，提升显著。另一项创新是流式TTS切分，将首帧延迟从约7700ms大幅降低，实现边生成边播放的效果。

---

## Slide 7 · 章节页：系统设计与核心算法（5秒）

### 口头稿

接下来介绍系统设计与核心算法。

---

## Slide 8 · 系统设计（35秒）

### 口头稿

本系统采用**三层分层架构**：前端交互层基于Vue3 + Live2D，负责页面渲染与语音采集；中间逻辑层以FastAPI为核心，整合GraphRAG检索、ASR/TTS语音处理和会话管理；后端数据层由三类数据库协同——一句话概括：**PostgreSQL存结构，Milvus查语义，Neo4j查图谱**，三库通过 `text_id` 字段跨库关联，实现数据实时一致。整个系统通过Docker Compose一键容器化部署。

---

## Slide 9 · 核心算法（45秒）

### 口头稿

本系统研发了三项核心算法。最核心的是GraphRAG混合检索，融合Milvus向量检索与Neo4j图检索，采用线性加权评分 $S = 0.6\,s_{vec} + 0.25\,s_{graph} + 0.15\,s_{ent}$，三个分量分别衡量语义相关性、图结构关联和实体一致性，召回率从66.7%提升到93.3%。第二项是多轮上下文维护，通过主景点综合得分模型识别对话主体，配合1200字符历史窗口裁剪，多轮识别准确率达66.7%到83.3%。第三项是流式TTS切分，按句末标点实时切分文本并异步并发合成，实现边生成边播放，首帧延迟从约7700ms大幅降低。

### 重点数据
- 召回率93.3%；回答准确率88.7%；答案相关性0.912；忠实度0.895
- 融合权重 α=0.60 / β=0.25 / γ=0.15

---

## Slide 10 · 章节页：系统功能演示（5秒）

### 口头稿

下面进行系统功能演示。

---

## Slide 11 · 功能① 智能导览问答（80秒）

### 口头稿

游客端采用**三栏布局**：左侧景区导航、中间Live2D数字人、右侧对话区，支持文字与语音双模态交互。核心是基于**SSE流式接口**的问答链路：用户问题 → Whisper ASR → GraphRAG混合检索 → LLM流式生成 → 讯飞TTS语音合成 → 前端实时播放，全程维护多轮上下文，不会"忘记"前面聊过的内容。下面演示。

### 演示步骤

1. 浏览器打开游客端主界面
2. 文本提问：**"蜀南竹海有哪些主要景点？"** — 观察Live2D口型与语音同步
3. 追问：**"其中哪个最值得游览？"** — 验证多轮上下文保持能力
4. 语音提问：**"开放时间是什么？"** — 测试ASR→TTS全链路

---

## Slide 12 · 功能② 景点知识库与检索（80秒）

### 口头稿

本页展示**GraphRAG双路检索**的透明化过程。管理后台实时显示每次检索的召回明细：Milvus向量库命中哪些片段及得分；Neo4j图数据库命中了哪些实体关系链；混合评分后选出最优上下文传给LLM。知识库管理方面，管理员新增一条景点描述后，系统自动完成三件事：向量化写入Milvus、解析实体写入Neo4j、结构化数据写入PostgreSQL，**三库实时一致**。下面演示。

### 演示步骤

1. 管理后台打开**"检索调试"面板**
2. 输入 "蜀南竹海有哪些景点"，观察双路召回明细与评分排名
3. 切换至**知识条目管理页**，新增一条景点描述
4. 观察后台自动入库日志，验证三库同步更新

---

## Slide 13 · 功能③ 数字人展示与交互（55秒）

### 口头稿

数字人模块使用Live2D Cubism Web SDK在浏览器端实时渲染，Vue3响应式状态管理支持角色热切换不刷新页面。系统内置三种导游角色，实现文本内容、语音音色、形象动作三者同步联动——甜美小妹绑定TTS音色"小露"，专业学者绑定"许久"，亲切导游绑定"小燕"，切换角色后讲解风格和音色随之同步变化。下面演示。

### 演示步骤

1. 游客端依次切换三个角色，向同一问题提问，对比TTS音色与讲解风格差异
2. 展示Live2D口型与语音实时同步效果

---

## Slide 14 · 功能④ 系统管理与权限（10秒）

### 口头稿

管理端支持数字人角色的完整增删改查，采用JWT令牌认证，游客端与管理端权限严格隔离。由于时间关系，该功能不作现场演示，如老师有兴趣可在提问环节进一步了解。

---

## Slide 15 · 章节页：总结与展望（5秒）

### 口头稿

最后做总结与展望。

---

## Slide 16 · 总结与展望内容（35秒）

### 口头稿

本系统构建了PostgreSQL、Neo4j与Milvus三库协同的异构数据架构，研发了GraphRAG混合检索、多轮上下文维护、流式TTS切分三项核心算法，并开发了覆盖游客端与管理端的完整Web系统，各项关键指标均达到设计目标。在展望方面，后续可从多景区知识库扩展、GraphRAG融合权重在线动态调优、以及轻量化模型的端侧部署三个方向持续优化。以上就是我的汇报，谢谢。

---

## Slide 17 · 致谢（10秒）

### 口头稿

感谢各位老师聆听，请批评指正。

---

## 答辩Q&A · 标准答案（提问环节备用，约5分钟）

---

### Q1：为什么要用GraphRAG？纯向量检索有什么问题？

纯向量检索只能做语义相似度匹配，景区问答有它覆盖不到的场景：

1. "景区有哪些景点" 需要图的 `HAS_SPOT` 关系，语义检索解决不了
2. 路线推荐需要遍历景点间关联，图结构天然支持多跳查询
3. 两个不同景区描述相似时，向量检索可能返回错误景区的内容，而 $s_{ent}$ 分量能过滤掉

实验结果：纯向量约66.7%，纯图约66.7%，混合**93.3%**，提升显著。

---

### Q2：融合权重 α=0.6, β=0.25, γ=0.15怎么来的？

根据景区问答场景特点经验设定，并通过离线实验调优：
- 向量相似度（0.6）权重最高，语义匹配是最基础的相关性保障
- 图结构支持（0.25）次之，补充结构关联信息
- 实体一致性（0.15）作为辅助过滤

三个权重在 `_build_scored_context` 中会归一化，加和不为1也能正常运行。

---

### Q3：意图分类为什么不直接用LLM？

**延迟**是核心原因。LLM调用即使是轻量请求也有200ms–1s的网络往返。关键词分类0ms命中，覆盖大多数常见问题（路线/列举/详情/位置等）。只有关键词无法判断时才退化到LLM分类，且设置3秒超时，失败则默认 `general` 意图继续流程。

---

### Q4：会话历史不会越来越长撑爆context吗？

有两层限制：
1. `SessionService` 内部 `max_history=10`，超过20条消息时滚动截断
2. 发给LLM前，`_trim_conversation_history` 按**1200字符预算**再次裁剪，优先保留含景区名的消息

所以发给LLM的context始终有限且可控。

---

### Q5：指代消解怎么实现的？

检测到"这里""这个景区"等代词后，从近8条历史合并文本，用jieba提取名词/地名（词性ns/nr/nt/nz），过滤停用词和过短词，取最多5个实体，拼接到查询前：

```python
effective_query = f"{' '.join(resolved_entities[:2])} {query}"
```

相当于把代词替换成具体实体再检索。

---

### Q6：TTS为什么要并发控制？

讯飞TTS是云API，有并发限制。不加控制时，一个长回答可能切成十几句同时发出，触发API限流。用 `asyncio.Semaphore(4)` 控制最多4个并发请求，既利用并发提速，又不超限。

---

### Q7：Milvus和普通数据库做向量搜索有什么区别？

普通关系数据库存向量可以，但相似度搜索只能暴力遍历（O(n)），数据量大时极慢。Milvus使用IVF_FLAT索引，先把向量空间分成1024个桶（nlist=1024），检索时只探测最近的10个桶（nprobe=10），大幅减少计算量，支持毫秒级响应。

---

### Q8：Neo4j查询有没有注入风险？

有防注入处理。对于动态拼接关系类型的场景，用正则白名单校验：

```python
if re.match(r"^[A-Z_][A-Z0-9_]*$", rel_candidate):
    rel = rel_candidate
```

节点属性查询均使用参数化（`$name`），不拼接字符串。

---

### Q9：系统有哪些局限性？

1. **冷启动延迟**：Embedding模型首次加载约5–10秒（后台线程异步加载，请求用Event等待）
2. **知识库依赖人工录入**：准确性取决于管理员上传内容的质量
3. **并发能力有限**：定位为演示系统，未做水平扩展，低并发场景适用
4. **图谱覆盖度**：LLM结构化提取在格式不规范文本上可能漏字段

---

## 关键代码速查

---

### 功能① 智能导览问答

#### 1-A 角色音色加载 · [rag.py:118](backend/app/api/rag.py#L118)

```python
# 每次问答前根据 character_id 从数据库读取提示词和音色
async def _load_character_prompt_and_voice(character_id):
    prisma = await get_prisma()
    character = await prisma.character.find_unique(where={"id": character_id})
    prompt = character.prompt if character.prompt else None
    voice  = character.voice  if character.voice  else settings.XFYUN_VOICE
    return prompt, voice
```

#### 1-B 多轮历史裁剪 · [rag.py:28](backend/app/api/rag.py#L28)

```python
_HISTORY_BUDGET_CHARS = 1200   # 发送给LLM前的对话历史字符预算
MAX_RECENT_HISTORY    = 20

def _trim_conversation_history(history, *, budget_chars=_HISTORY_BUDGET_CHARS,
                                prefer_terms=None):
    recent = [m for m in history if m.get("role") in ("user", "assistant")]
    recent = recent[-MAX_RECENT_HISTORY:]        # 第一层：最多取最近20条
    flags  = [_hit(m) for m in recent]           # 标记含景区名等关键词的消息

    keep, used = set(), 0
    for i in range(len(recent) - 1, -1, -1):     # 第二层①：优先保留含关键词的消息
        if not flags[i]: continue
        ln = _len_msg(recent[i])
        if used + ln > budget_chars: continue
        keep.add(i); used += ln
    for i in range(len(recent) - 1, -1, -1):     # 第二层②：剩余预算从最近向前补充
        if i in keep: continue
        ln = _len_msg(recent[i])
        if used + ln > budget_chars: continue
        keep.add(i); used += ln
    return [recent[i] for i in range(len(recent)) if i in keep]
```

#### 1-C 主景点综合得分 · [rag.py:190](backend/app/api/rag.py#L190)

```python
wq, wr, ws = 1.0, 0.6, 0.8   # 问题提及权重 / 回复提及权重 / 景区归属权重

for rid, name in _get_attraction_id_name_list():
    cnt_q = q.count(name)     # 问题中景点名出现次数
    cnt_r = r.count(name)     # 回复中景点名出现次数
    if cnt_q == 0 and cnt_r == 0:
        continue
    # 通过Neo4j查询HAS_SPOT关系判断景点是否属于当前景区
    scenic_hit = 1 if int(rid) in scenic_aid_set else 0
    score = wq * cnt_q + wr * cnt_r + ws * scenic_hit
    if score > best_score:
        best_score = score
        best_id    = int(rid)   # 综合得分最高即为当前主景点
```

#### 1-D 流式TTS切分 + 异步并发 · [rag.py:428](backend/app/api/rag.py#L428)

```python
Lmin = 12                           # 触发切分的最短字符数
TTS_MAX_CONCURRENCY = 3
tts_sema = asyncio.Semaphore(TTS_MAX_CONCURRENCY)
SENTENCE_PUNCT = ['。', '！', '？', '.', '!', '?']

def _compute_cut(buf: str) -> Optional[int]:
    last = -1
    for p in SENTENCE_PUNCT:
        last = max(last, buf.rfind(p))
    if last >= 0:
        return last + 1            # 句末标点后切断
    if len(buf) >= Lmin:
        return len(buf)            # 无标点但够长时强切
    return None

async def synthesize_and_store(idx: int, original_txt: str) -> None:
    async with tts_sema:           # 最多3路并发合成
        path = await asyncio.wait_for(
            voice_service.synthesize_xfyun(txt, voice=voice),
            timeout=TTS_SENTENCE_TIMEOUT,
        )
        if path and _validate_wav_file(path):
            with open(path, "rb") as f:
                b64 = base64.b64encode(f.read()).decode("utf-8")
            completed_audio[idx] = b64  # 存入完成队列，前端按序取走播放
```

---

### 功能② 景点知识库与检索

#### 2-A 数据清洗：LLM结构化解析 · [rag_service.py:226](backend/app/services/rag_service.py#L226)

原始景区/景点文本经LLM解析为标准JSON，消除字段不规范、实体歧义等噪声。

```python
# parse_scenic_text —— 景区文本结构化
system_prompt = """
你是景区知识结构化助手。统一命名规范（用于控制图谱节点）：
1）scenic_spot：输出景区正式全称，例如"蜀南竹海景区"
2）spots：子景点常用短名，例如"海中海""观光索道"，不要带"景点""景区"等后缀
3）features/awards：简洁名词短语，不要写成长句
返回字段：scenic_spot / location / area / features / spots / awards /
          ticket_info / opening_hours / transport_info / service_facilities
只输出JSON，不要解释。"""

resp = self.llm_client.chat.completions.create(
    model=settings.OPENAI_MODEL,
    messages=[{"role": "system", "content": system_prompt},
              {"role": "user",   "content": text}],
    temperature=0.1, max_tokens=512,
)
data = json.loads(_extract_json_str(resp.choices[0].message.content))
```

```python
# build_attraction_cluster 内的实体过滤（graph_builder.py 296–313）
for entity in entities:
    entity_name = str(entity.get("text") or "").strip()
    if not entity_name or len(entity_name) < 2 or entity_name.isdigit():
        continue              # 过滤单字、纯数字
    if entity_name in seen:
        continue              # 去重
    seen.add(entity_name)
    # 实体类型白名单：LOCATION / PERSON / ORG / KEYWORD / ENTITY
    entity_type = entity.get("type", "KEYWORD")
    if entity_type not in ("LOCATION","PERSON","ORG","OTHER","KEYWORD","ENTITY"):
        entity_type = "KEYWORD"
```

```python
# LLM输出清洗（rag_service.py 78–128）
def _strip_emoji(text):
    s = re.sub(r"[☀-⛿✀-➿\U0001F300-\U0001F9FF]", "", text)
    s = re.sub(r"[~～〜]", "", s)
    s = re.sub(r"\s{2,}", " ", s).strip()
    if s and s[-1] not in "。！？.!?…":
        s = s.rstrip("，、；：") + "。"   # 末尾补句号，避免TTS异常
    return s

def _clean_special_symbols(text):
    s = re.sub(r"\*\*([^*]+)\*\*", r"\1", text)   # 去Markdown加粗
    s = re.sub(r"#+\s*", "", s)                    # 去标题符
    s = re.sub(r"^[\s]*[-•▪]\s+", "", s, flags=re.MULTILINE)  # 去列表符
    s = re.sub(r"\s{2,}", " ", s).strip()
    return s
```

---

#### 2-B 数据导入：Milvus向量入库 · [admin.py:1036](backend/app/api/admin.py#L1036)

```python
async def _upload_items_to_graphrag(items, collection_name, build_graph, ...):
    collection = milvus_client.create_collection_if_not_exists(
        collection_name, dimension=384)           # 384维，paraphrase-multilingual-MiniLM-L12-v2

    # 批量Embedding
    texts = [item.text for item in items]
    embeddings = await asyncio.to_thread(
        rag_service.generate_embeddings_batch, texts)

    entities = [
        [item.text_id for item in items],         # 字段①：text_id（如 kb_001）
        [item.text[:65535] for item in items],     # 字段②：原始文本
        embeddings,                                # 字段③：384维向量
    ]
    for tid in entities[0]:
        collection.delete(f'text_id == "{tid}"')  # 先删旧数据，保证幂等
    collection.insert(entities)
    collection.flush()
```

---

#### 2-C 数据导入：Neo4j图谱建簇 · [graph_builder.py:45](backend/app/services/graph_builder.py#L45)

```python
async def build_attraction_cluster(self, attraction_data, text_id, text, parsed):
    # ① MERGE景点中心节点
    await self._execute_query_async("""
        MERGE (a:Attraction {id: $id})
        SET a.name=$name, a.description=$description,
            a.location=$location, a.category=$category
    """, {...})

    # ② 建景区归属关系（双向）
    await self._execute_query_async("""
        MATCH (a:Attraction {id: $id})
        MATCH (s:ScenicSpot {scenic_spot_id: $scenic_spot_id})
        MERGE (a)-[:属于]->(s)
        MERGE (s)-[:HAS_SPOT]->(a)
    """, {"id": att_id, "scenic_spot_id": scenic_spot_id})

    # ③ 建特色/荣誉辐射节点
    await self._execute_query_async("""
        UNWIND $features AS fname
        MATCH (a:Attraction {id: $id})
        MERGE (f:Feature {name: fname})
        MERGE (a)-[:HAS_FEATURE]->(f)
    """, {"id": att_id, "features": feats})

    # ④ 建文本节点并关联
    await self._execute_query_async("""
        MERGE (t:Text {id: $text_id})
        MATCH (a:Attraction {id: $id})
        MERGE (t)-[:DESCRIBES]->(a)
    """, {"text_id": text_id, "id": att_id})

    # ⑤ jieba实体提取后建MENTIONS关系
    for entity in rag_service.extract_entities(text):
        await self._execute_query_async(f"""
            MATCH (a:Attraction {{id: $id}})
            MERGE (e:{entity_type} {{name: $name}})
            MERGE (a)-[:MENTIONS]->(e)
        """, {"id": att_id, "name": entity_name})
```

---

#### 2-D Milvus向量检索 · [rag_service.py:482](backend/app/services/rag_service.py#L482)

```python
async def vector_search(self, query, collection_name, top_k):
    query_vector = [self.generate_embedding(query)]  # 查询文本Embedding
    search_params = {
        "metric_type": "L2",          # L2距离（欧氏距离）
        "params": {"nprobe": 10},     # 探测最近10个IVF桶（nlist=1024）
    }
    results = collection.search(
        data=query_vector,
        anns_field="embedding",
        param=search_params,
        limit=top_k,
        output_fields=["text_id", "content"],
    )
    # L2距离 → 相似度分：distance越小越相似
    for hit in results[0]:
        score = 1 / (1 + hit.distance) if hit.distance > 0 else 1.0
```

---

#### 2-E Neo4j图检索 · [rag_service.py:622](backend/app/services/rag_service.py#L622)

```python
async def graph_search(self, entity_name, relation_type=None, limit=10):
    # relation_type白名单校验，防注入
    if relation_type:
        if re.match(r"^[A-Z_][A-Z0-9_]*$", relation_type.strip().upper()):
            rel = relation_type.strip().upper()

    query = f"""
    MATCH (a)-[r:{rel}]->(b)
    WHERE a.name CONTAINS $name OR b.name CONTAINS $name
    RETURN a, r, b, labels(a), labels(b), type(r)
    LIMIT $limit
    """

async def _graph_search_many(self, entity_names, per_entity_limit=5):
    # 批量查询，一次round-trip查多个实体
    query = """
    UNWIND $names AS name
    CALL {
      WITH name
      MATCH (a)-[r]->(b)
      WHERE a.name CONTAINS name OR b.name CONTAINS name
      RETURN a, r, b, labels(a) as a_labels, labels(b) as b_labels, type(r) as rel_type
      LIMIT $per_limit
    }
    RETURN name as query_name, a, r, b, a_labels, b_labels, rel_type
    """
```

---

#### 2-F 混合检索主流程 · [rag_service.py:1257](backend/app/services/rag_service.py#L1257)

```python
async def hybrid_search(self, query, top_k=5, conversation_history=None, scenic_name=None):
    # ① 指代消解：把"这里""这个景区"替换为真实实体名
    if self._has_pronoun_reference(query) and conversation_history:
        resolved_entities = self._extract_entities_from_history(conversation_history)
    effective_query = f"{' '.join(resolved_entities[:2])} {query}"

    # ② 并行：意图分类 + 向量检索
    intent, vector_results_raw = await asyncio.gather(
        self._classify_query_intent(query),   # LLM分类：route/listing/detail/...
        self.vector_search(effective_query, top_k=10),
    )

    # ③ 按意图调整策略（top_k、相关性阈值、图查询深度）
    strategy = self._get_search_strategy(intent)
    # listing/route意图 → 更大top_k、更深图查询
    # detail意图 → 较小top_k、精准匹配

    # ④ 并行：图关系检索 + 子图搜索
    graph_results, subgraph_data = await asyncio.gather(
        self._graph_search_many(entity_names[:5], per_entity_limit=per_entity_limit),
        self.graph_subgraph_search(entity_names[:3], depth=graph_depth),
    )

    # ⑤ 融合评分，选出最优上下文片段
    enhanced_results, scored_candidates = self._build_scored_context(
        vector_results=vector_results,
        graph_results=graph_results,
        entity_names=entity_names,
        resolved_entities=resolved_entities,
        scenic_name=scenic_name_str,
    )
```

---

#### 2-G 融合评分公式 · [rag_service.py:1955](backend/app/services/rag_service.py#L1955)

```python
DEFAULT_FUSION_ALPHA = 0.60   # svec：向量语义相关性
DEFAULT_FUSION_BETA  = 0.25   # sgraph：图结构关联
DEFAULT_FUSION_GAMMA = 0.15   # sent：实体一致性（防跨景区混淆）

# _build_scored_context 内
a, b, g = alpha/ssum, beta/ssum, gamma/ssum   # 归一化三权重

for item in vector_results:
    svec  = 1 / (1 + distance)                # L2距离换算相似度

    hits  = sum(1 for an, bn in rel_pairs
                if (an and an in content) or (bn and bn in content))
    sgraph = min(1.0, hits / max(3, len(rel_pairs)*2))  # 图实体命中覆盖率

    sent = 0.0
    if scenic_name and scenic_name in content:
        sent = 1.0          # 景区名命中 → 满分
    elif any(e in content for e in resolved):
        sent = 1.0          # 消解实体命中 → 满分
    elif any(e in content for e in entities[:5]):
        sent = 0.5          # 备用实体命中 → 半分

    S = a*svec + b*sgraph + g*sent   # 线性加权综合得分
```

---

### 功能③ 数字人展示与交互

数字人的核心逻辑在前端（`frontend-tourist/src/lib/live2d/`），后端只负责提供角色配置（见功能① 1-A）和TTS音频流（见功能① 1-D）。

后端角色配置关键字段（`characters`表）：

```python
# characters.py 103–136  create_character
# live2d_character_name: Live2D模型ID（Mao / Chitose / Tsumiki 等）
# voice: 讯飞TTS音色（x4_xiaoyan / x4_xiaomei / x4_xiaoling 等）
# prompt: System Prompt，控制讲解风格
# is_active: 启用开关，即时生效
```

---

### 功能④ 系统管理与权限

#### 4-A JWT令牌验证（依赖注入） · [auth.py:37](backend/app/api/auth.py#L37)

```python
async def get_current_user(token: str = Depends(oauth2_scheme),
                           db: Session = Depends(get_db)):
    payload = decode_access_token(token)
    if payload is None:
        raise HTTPException(401, "无效的认证凭据",
                            headers={"WWW-Authenticate": "Bearer"})
    username = payload.get("sub")
    user = db.query(User).filter(User.username == username).first()
    if user is None:
        raise HTTPException(401, "无效的认证凭据",
                            headers={"WWW-Authenticate": "Bearer"})
    return user
```

#### 4-B JWT令牌签发 · [auth.py:97](backend/app/api/auth.py#L97)

```python
@router.post("/login")
async def login(form_data: OAuth2PasswordRequestForm = Depends(),
                db: Session = Depends(get_db)):
    user = db.query(User).filter(User.username == form_data.username).first()
    if not user or not verify_password(form_data.password, user.hashed_password):
        raise HTTPException(401, "用户名或密码错误",
                            headers={"WWW-Authenticate": "Bearer"})
    access_token = create_access_token(
        data={"sub": user.username, "is_admin": user.is_admin},
        expires_delta=timedelta(hours=24)   # 令牌有效期24小时
    )
    return TokenResponse(access_token=access_token, token_type="bearer", ...)
```

#### 4-C 管理端权限检查模式 · [admin.py:295](backend/app/api/admin.py#L295)

```python
# 所有管理端接口通过 Depends(get_current_user) 自动验证JWT
# 需要管理员身份的接口额外检查 is_admin 标志
@router.post("/scenic-spots")
async def create_scenic_spot(
    ...,
    current_user: User = Depends(get_current_user)   # JWT验证
):
    if not current_user.is_admin:
        raise HTTPException(status_code=403, detail="需要管理员权限")
    # 游客端只能访问问答与语音接口，无法通过此检查
    ...
```

