# 答辩准备 · 基于AI数字人和知识库的景区导游系统

> **答辩人：** 方家伟 · 计算机学院 软件工程专业 · 指导老师：王雪松

---

## 一、创新点

### 创新点① — GraphRAG 混合检索

**一句话：** 向量检索 + 图检索双路融合，召回率从 66.7% 提升到 93.3%。

**运行流程：** 查询文本先经句向量模型编码，Milvus 用 IVF_FLAT 索引（nlist=1024，nprobe=10）做 Top-K 语义检索，L2 距离转相似度分：

$$s_{vec}(c)=\frac{1}{1+d(c)}$$

按意图策略阈值 τ 筛选候选集，若为空则强制保留最高分一条防止召回为空。随后从问题与候选内容中抽取实体，Neo4j 围绕实体做有深度限制的关系查询，计算图关系命中覆盖率 $s_{graph}$。融合阶段对每个候选片段计算综合得分：

$$S(c)=\alpha\,s_{vec}(c)+\beta\,s_{graph}(c)+\gamma\,s_{ent}(c)$$

α=0.60 / β=0.25 / γ=0.15，权重经 15 道分层测试题离线调优，三权重在代码里归一化，加和不为 1 也正常运行。候选集按 S(c) 排序，在字符预算 B=2500 内选取高分片段组装上下文：

$$\text{Context}=\{c\in\text{Top}(S)\mid|c|\leq B\}$$

纯向量只能做语义匹配，"景区有哪些景点"这类需要遍历 `HAS_SPOT` 图关系的问题它答不了；纯图检索又没有语义理解能力。两路融合互补，实验结果混合 93.3% vs 纯向量/纯图均约 66.7%。意图分类七类（route/listing/detail/comparison/nearby/temporal/general）动态调整 top-k 和图查询深度；向量编码引入带 TTL 的本地缓存减少重复调用。

**权重被问时：** α=0.6（语义最基础）、β=0.25（图结构补充关联）、γ=0.15（辅助过滤跨景区噪声），$s_{ent}$：景区名命中得 1.0，消解实体命中得 1.0，备用实体命中得 0.5。

**代码入口：** [rag_service.py:1257](backend/app/services/rag_service.py#L1257) `hybrid_search` → 融合评分 [rag_service.py:1955](backend/app/services/rag_service.py#L1955) `_build_scored_context` → 权重常量 [rag_service.py:33](backend/app/services/rag_service.py#L33)

---

### 创新点② — 流式 TTS 文本切分

**一句话：** 遇到句末标点就切、异步并发合成，首帧延迟从约 7700ms 大幅降低。

**切分规则：** 维护累积缓冲文本 T，设句末标点位置集合为 P，最小长度阈值 $L_{min}=12$：

$$cut=\begin{cases}\max(P)+1 & P\neq\emptyset \\ |T| & P=\emptyset \text{ 且 } |T|\geq L_{min}\end{cases}$$

切出子句 $u=T[0:cut]$ 提交 TTS，剩余 $T=T[cut:]$ 继续累积；两个条件均不满足则继续等待。原来的做法是等 LLM 把完整回答全部生成完再整段提交，一篇 400 字的回答要等约 7700ms 才能开口说话。改进后第一个子句一切出就立即提交合成，合成完前端立刻播放，不等后续。

**并发控制：** 最多 3 路并发 TTS 请求（asyncio.Semaphore(3)），前端收到音频 base64 即刻播放。

**重试机制：** 仅对网络超时/连接错误等可恢复异常重试，最多 3 次，指数退避：

$$\Delta t_i=\Delta t_0\cdot 2^{i-1},\quad\Delta t_0=1.0\text{s},\quad i=1,2$$

鉴权失败（401/403）直接抛出不重试。合成完成后校验音频时长（0.15–120s）和文件大小（>800 字节），异常片段退回纯文本展示。

**代码入口：** [rag.py:436](backend/app/api/rag.py#L436) `_compute_cut`（切分逻辑）→ [rag.py:469](backend/app/api/rag.py#L469) `synthesize_and_store`（并发合成）→ [voice_service.py:418](backend/app/services/voice_service.py#L418) 指数退避重试 → [voice_service.py:166](backend/app/services/voice_service.py#L166) 讯飞 WebSocket TTS 实现

---

### 创新点③ — 多轮上下文维护

**一句话：** 主景点综合得分识别对话主体 + 1200 字符历史裁剪，多轮识别准确率 66.7%–83.3%。

**主景点识别：** 每轮问答后扫描问题 q 和回答 r，对候选集 A 中每个景点计算综合得分：

$$Score(a_i)=w_q\cdot cnt(a_i,q)+w_r\cdot cnt(a_i,r)+w_s\cdot\mathbb{I}_{a_i\in Scenic}$$

$w_q=1.0$（问题提及权重）/ $w_r=0.6$（回复提及权重）/ $w_s=0.8$（景区归属权重），取得分最高者为主景点：

$$a^*=\arg\max_{a_i\in A}Score(a_i)$$

候选集 A 为 q 或 r 中至少出现一次名称的景点子集；q 和 r 均未命中则不强行标注。用户下次说"这里""这附近"时，代词消解从最近 8 条历史提取实体名拼到查询前，相当于把代词替换成具体景点名再检索。

**历史裁剪：** 会话历史送入 LLM 前按字符预算 L=1200 裁剪：

$$H'\subseteq H,\quad s.t.\sum_{m_i\in H'}|m_i|\leq L$$

两轮遍历：第一轮从后往前优先保留含景区名关键词的消息，预算未满再第二轮补入其余消息，保证 LLM 收到的上下文既有限又保留最关键信息。第一层还有 max_history=20 的硬截断兜底。

**代码入口：** [rag.py:190](backend/app/api/rag.py#L190) 主景点评分 → [rag.py:39](backend/app/api/rag.py#L39) `_trim_conversation_history` 历史裁剪 → 裁剪预算常量 [rag.py:28](backend/app/api/rag.py#L28)

---

## 二、演示功能

### 功能① — 智能导览问答

**演示流程：** 打开游客端主界面（三栏布局：左导航 / 中 Live2D 数字人 / 右对话区）→ 文字提问"蜀南竹海有哪些主要景点" → 追问"其中哪个最值得游览"验证上下文保持 → 语音提问"开放时间"走完 ASR→GraphRAG→LLM→TTS 全链路。

**完整技术链路：** 前端录音上传 WAV → 后端 `POST /voice/transcribe` 用 Whisper base 模型识别为文字（[voice_service.py:63](backend/app/services/voice_service.py#L63)）→ `POST /rag/generate-stream` SSE 接口进入混合检索 → LLM 流式生成 → 边生成边切分提交讯飞 TTS → 前端收到音频 base64 立即播放，同时数字人口型同步。每次问答前根据 character_id 加载该角色的提示词和音色配置（[rag.py:118](backend/app/api/rag.py#L118)）。

**被问到 Whisper 时：** 模型加载在服务启动时完成（[voice_service.py:51](backend/app/services/voice_service.py#L51)），识别时用 `run_in_executor` 包装成异步不阻塞主线程，前端上传的音频落盘到系统临时目录，识别完立即删除（[voice.py:110](backend/app/api/voice.py#L110)）。

---

### 功能② — 景点知识库与检索

**演示流程：** 管理后台打开"检索调试"面板 → 输入"蜀南竹海有哪些景点" → 实时查看双路召回明细（Milvus 向量命中条目 + 相似度分值、Neo4j 图检索命中实体关系链）→ 观察混合评分排序 → 切换到知识条目管理页新增一条景点描述 → 验证后端自动向量化入库并同步更新 Neo4j 图谱。

**三库写入流程（新增一条知识时）：** 管理员提交文本 → `_upload_items_to_graphrag`（[admin.py:1029](backend/app/api/admin.py#L1029)）：① 调用 Embedding 模型生成 384 维向量 → `collection.insert` 写入 Milvus（[admin.py:1053](backend/app/api/admin.py#L1053)）；② LLM 结构化解析文本为 JSON（[rag_service.py:226](backend/app/services/rag_service.py#L226)）→ `build_scenic_cluster` 在 Neo4j 建景区簇节点和关系（[graph_builder.py:45](backend/app/services/graph_builder.py#L45)）；③ PostgreSQL 由 Prisma ORM 在各业务接口独立维护，是数据源。三步顺序执行，任一失败返回错误，管理员重试可重新同步。

**被问到一致性时：** 顺序写入 + 失败返回错误，最终一致性而非强一致性，原型系统的工程权衡，知识库人工维护频率低，偶发失败手动重试即可。

---

### 功能③ — 系统管理与权限

由于时间关系此功能不作现场演示，核心是两点：JWT 令牌认证（[auth.py:37](backend/app/api/auth.py#L37) `get_current_user` 作为 FastAPI 依赖注入到所有管理端接口），以及游客端和管理端权限硬隔离（游客只能访问问答与语音接口，管理端接口额外校验 `is_admin` 标志）。数字人角色支持完整增删改查，每个角色可独立配置讲解风格、TTS 音色、系统提示词和 Live2D 形象，启用开关即时生效。

---

