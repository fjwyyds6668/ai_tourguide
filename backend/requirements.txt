# 构建工具（Python 3.13 需要）
setuptools>=69.0.0
wheel>=0.42.0

# 数据处理（需要先安装，因为其他包依赖它们，Python 3.13 兼容版本）
numpy>=1.26.0
pandas>=2.1.4

# FastAPI 核心（升级到支持 Python 3.13 的版本）
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0

# 数据库
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
neo4j==5.14.1
pymilvus==2.3.4

# 语音处理
openai-whisper>=20231117
vosk==0.3.45
azure-cognitiveservices-speech==1.32.1
edge-tts==6.1.9

# AI 和向量
openai==1.3.7
langchain==0.0.350
langchain-community==0.0.10
sentence-transformers==2.2.2

# 中文 NLP（用于实体识别）
jieba==0.42.1
# pkuseg==0.0.25  # 已移除：代码中未使用，且与 Python 3.13 不兼容

# 工具库
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0
httpx==0.25.2
aiofiles==23.2.1

